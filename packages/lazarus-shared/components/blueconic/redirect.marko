import { getAsObject } from "@base-cms/object-path";
import defaultValue from "@base-cms/marko-core/utils/default-value";
import blueconic from "../../blueconic";

$ const { site } = out.global;

$ const { redirectTo, librarySrc, enabled, debug } = input;
$ const timeout = defaultValue(input.timeout, 2000);

<if(enabled)>
  $ const props = { redirectTo, librarySrc, timeout, debug };
  <marko-web-browser-component name="LazarusSharedBlueConicRedirect" props=props />
</if>
